{
  "name": "serial-decoder",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/split-decoder-data.js && node scripts/generate-seo-pages.js",
    "dev": "npx serve .",
    "test": "node tests/decoder-regressions.test.mjs",
    "refresh-pricebook": "node scripts/refresh-pricebook.js"
  },
  "dependencies": {
    "@upstash/ratelimit": "^2.0.8",
    "@upstash/redis": "^1.36.2"
  }
}
