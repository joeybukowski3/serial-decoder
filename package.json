{
  "name": "serial-decoder",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/generate-seo-pages.js",
    "dev": "npx serve .",
    "test": "node tests/decoder-regressions.test.mjs",
    "refresh-pricebook": "node scripts/refresh-pricebook.js"
  },
  "dependencies": {
    "@upstash/ratelimit": "^2.0.8",
    "@upstash/redis": "^1.36.2"
  },
  "devDependencies": {
    "clean-css-cli": "^5.6.3",
    "terser": "^5.48.0"
  }
}
