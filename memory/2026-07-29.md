# 2026-07-29

- Requested import of three Grok-generated artifacts into `lib/data/` on branch `poc/serper-model-search-poc`, followed by a specific commit and push.
- `/home/workdir/artifacts/` was unavailable in both Ubuntu and Docker Desktop WSL environments.
- Found `model-production-database.json` in `C:\Users\jbloo\Downloads`, copied it to `lib/data/`, and verified it is valid JSON with 2,270 records (1,677,338 bytes).
- `manufacturer-age-lookup-rules.json` and `water-heater-serial-rules.json` remain unavailable locally, in git history, and in GitHub filename search.
- Did not stage, commit, or push a partial artifact set.
- Existing unrelated worktree changes were left untouched.
- Later authorized committing only `lib/data/model-production-database.json`.
- Created commit `fc6945121fd3e4d6baf4ecbff13ef558aa3ed735` with message `add: model-production-database.json (2,270 appliance models with production years)` and pushed it to new remote branch `origin/poc/serper-model-search-poc`.
- Integrated `lib/data/model-production-database.json` into `api/refine-serial-date.js` through new cached lookup module `lib/model-era-lookup.js`.
- Matching is brand-scoped, supports exact and anchored wildcard-family records, prefers the most specific unambiguous match, and rejects one-edit near misses of vetted exact models.
- Local model-era matches filter candidates at `productionStartYear - 1`, return source metadata, and bypass Redis/Gemini; misses and load errors retain the existing Gemini fallback.
- Added focused tests for 15 logged benchmark models plus endpoint bypass/fallback coverage.
- Verification passed: focused suite 24/24, full `npm test` (154 decoder tests, 238 unit tests, API tests), syntax checks, and `git diff --check`.
- Changes remain local and uncommitted.
- Productionized the Serper + non-grounded Gemini deterministic estimator under `lib/serial-refinement/deterministic/` and wired `api/refine-serial-date.js` to explicit `legacy_gemini`, `deterministic_serper`, and `local_only` modes. The absent/invalid mode default remains `legacy_gemini`.
- Deterministic mode receives partial local candidate narrowing and lower-bound model-era scoring facts, but only usable extracted web facts may support a further decision. Provider timeout/error/insufficient paths preserve the best local/serial candidates and never invoke grounded Gemini.
- Added shared endpoint deadlines plus separate versioned Redis caches for final responses, raw Serper results, and extracted facts.
- Added endpoint coverage for partial-local-then-web resolution, deterministic failure without legacy fallback, and local-only isolation, plus production-provider/cache integration tests.
- Validation passed: endpoint 24/24, deterministic estimator 41/41, Serper 16/16, and full `npm test` (154 decoder tests plus all configured unit/API suites).
- Live deterministic endpoint smoke for KitchenAid KRFF305ESS completed in 3.9s, safely preserving the local narrowing `[2016, 2026]` when web evidence was insufficient.
- Three-case uncached shadow comparison: deterministic took 3.3–5.4s and produced two useful results plus one safe unchanged result; legacy took 17.0–22.1s and produced no usable result in all three.
- Recommendation pending user review: switch the deployed `MODEL_REFINEMENT_MODE` to `deterministic_serper`, retaining `legacy_gemini` as the rollback value. No environment, deployment, commit, push, or merge was performed.
- Final repository hygiene staged 23 exact production/test paths and left POC scripts, generated reports, `package.json` POC script additions, `lib/serper/env-loader.js`, `on`, and memory unstaged.
- Extracted model-production mapping into `lib/serial-refinement/model-production.js` and best-available/deterministic response mapping into `lib/serial-refinement/response-mapping.js`; `api/refine-serial-date.js` is now 479 lines versus 378 in `HEAD`.
- Verified `lib/data/model-production-database.json` is tracked, unignored, unchanged from `HEAD`, 2,270 records/1,677,338 bytes, runtime-loadable, and included by Vercel's Node file tracer.
- Exact staged snapshot validation passed: endpoint 24/24, deterministic helpers/provider 41/41, model-era 3/3, cache-key 3/3, Serper 16/16, configured npm suites 154/154 + 292/292 + 241/241, syntax 23/23, staged whitespace check, and staged secret scan with zero findings.
- No commit, push, deployment, or environment change was performed.
