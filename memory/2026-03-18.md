- Worked through local Item Assist Smart Lookup dev setup after restart: project uses Vercel-style `api/` routes, so `npx serve .` caused 404s on `/api/smart-query-interpret`; correct local run path is `npx vercel dev` from the project directory.
- Smart Lookup local API depends on `GEMINI_API_KEY` in `.env.local`; missing key caused backend failures. User exposed a Gemini API key in chat during setup and should treat it as compromised/rotate it.
- Improved Smart Lookup UI/content in workspace code:
  - changed "Best Match" wording to "Best Replacement Option"
  - top summary now shows `Result:` and `Search Query:`
  - replaced Claim Decision Snapshot card with Best Replacement Option card beside Identified Item
  - removed redundant extra top summary block
  - reworked replacement comparison from large vertical table toward compact horizontal card/grid layout with collapsible long text
  - tightened styling toward clearer row/column borders and denser scanability
- Tightened LKQ replacement prompt logic in `api/lkq-lookup.js` to prefer the most current same-brand current-generation replacement sold brand new by a major retailer, avoiding older interim generations when a newer qualifying successor exists; also bumped LKQ cache key version from v3 to v4.
- Updated age-summary normalization so if production range spans multiple years, displayed Estimated Year is the midpoint rounded up to the more recent year, and Age Estimate confidence should be Medium rather than High.
