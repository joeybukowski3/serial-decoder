# 2026-07-30

- Implemented local-only Phase 1 model-evidence policy hardening on `feat/serper-model-era-poc`.
- Publication/listing/manual/review/troubleshooting dates are non-directional existence evidence; only explicit lifecycle date meanings can create production bounds.
- Added deterministic exact/variant/family/mismatch model identity classification. Gemini exactness is retained only as a suggestion and cannot upgrade deterministic identity.
- Era clustering now requires two independent exact-model domains and at least one lifecycle fact; era-influenced scoring cannot bypass resolution guards through elimination.
- Bumped deterministic extracted-facts cache version to 2. Production mode remains `legacy_gemini`; no feature flag, endpoint routing, deployment, merge, push, or protected artifact changes.
- Verification: focused deterministic tests 45/45, `npm test` passed, Smart Lookup unit tests 330/330.
