## 2026-05-14

- Removed the duplicate `hero-stats` badge block from `index.html` above the search panel.
- Left the lower trust/stats bar below the search panel unchanged.
- Verified with `npm run build` (`node scripts/generate-seo-pages.js`), which completed successfully and regenerated SEO pages plus `sitemap.xml`.
