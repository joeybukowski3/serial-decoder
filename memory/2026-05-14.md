## 2026-05-14

- Removed the duplicate `hero-stats` badge block from `index.html` above the search panel.
- Left the lower trust/stats bar below the search panel unchanged.
- Verified with `npm run build` (`node scripts/generate-seo-pages.js`), which completed successfully and regenerated SEO pages plus `sitemap.xml`.
- Updated `script.js` so standalone `smart-lookup.html` keeps the submitted query visible, while the homepage Smart Lookup input still clears after submit.
- Added a fallback scroll in `showAgeLookupResults()` so standalone Smart Lookup results scroll into view even without `#ageResults`.
- Updated the standalone Smart Lookup “Add Context” tip copy and switched its footer links to `/methodology`, `/contact`, and `/security`.
- Added `serial-number-location-guide.html`, a long-form SEO page covering appliance, HVAC, water-heater, and electronics serial-number label locations with article and FAQ schema.
- Updated `vercel.json` with a `/serial-number-location-guide` rewrite and updated `scripts/generate-seo-pages.js` so `where-is-my-serial-number.html` now includes a prominent link to the new guide and `sitemap.xml` keeps the new route after builds.
- Verified with `npm run build`, which passed and regenerated `where-is-my-serial-number.html` plus `sitemap.xml`.
