# 2026-07-12

## Smart Lookup year-context result semantics

- Started from fetched `origin/main` at `ba2bb66` in isolated worktree `C:\tmp\serial-decoder-year-context`; preserved the existing Richmond checkout and unrelated local files.
- Created branch `feature/smart-lookup-year-context` and commit `232b5ba5d13443c7db5f5fee1df2835ea77592e1` (`feat: promote Smart Lookup year context`). Pushed the branch to `origin` with upstream tracking after Joey explicitly requested it; no deployment.
- Root cause: LG family recall exposed `modelYearFamilyYear` and descriptive copy, but the normalized result had no usable year field; `hasUsableAgeInfo` only recognized introduction/manufacture/range fields, so the controller rendered product-family recognition as a no-result card.
- Added normalized `yearContext` with validated type, label, confidence, source, exact-unit flag, scalar year or range. Model/family years remain separate from `individualManufactureYear`.
- LG C3/C2/B3/G3 deterministic results now return high-confidence local-seed model-year-family context. Exact LG OLED models preserve exact model and screen size but do not claim unit manufacture dates.
- Samsung Q60 broad searches return a structured 2019-2024 variant range with Q60R/Q60RA through Q60D year options; Q60A returns 2021 without promoting the partial token to an exact model.
- UI treats supported `yearContext` as success and renders a large primary year/range, semantic label, brand/family/model/manufacture status, supporting copy, variants, and next action. Unknown-year recognized products get specific "Product recognized, year not verified yet" copy.
- Validation passed: Smart unit 66, Smart API 21, Smart benchmark 1, full `npm test` (129 decoder + 35 refinement + 34 API), production build, all requested syntax checks, and diff check.
- Smart Playwright passed 15/15 once. Required live browser queries were then verified through Vercel dev; a later final full-suite rerun was interrupted by recurring host Windows `CreateProcessAsUserW 1312`, while targeted post-adjustment browser verification still passed.
- Fixed the subsequent Smart Lookup GitHub Actions browser failure on the same branch. Root cause was test-only: the two year-context Playwright tests called `page.unroute('**/api/age-lookup')`, but CI serves static files with `python3 -m http.server`, so the real API request could never succeed. Added fully mocked query-aware year-context fixtures, `**/api/age-lookup*` interception, URL/method validation, and explicit per-query interception assertions. Fixtures include the complete LG/Samsung year-context contract, including explicit null manufacture fields. No production code changed. Final amended commit: `b1389aa` (`test: mock Smart Lookup year context in browser suite`), pushed to `origin/feature/smart-lookup-year-context` for PR #38 after Joey explicitly requested it. Local Smart Playwright passed 15/15; all other requested suites/build/checks passed. No merge.
