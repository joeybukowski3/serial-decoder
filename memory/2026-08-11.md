# 2026-08-11

## Brand page desktop layout consistency
- Root cause: brand decoder pages mixed shell widths (hero ~720, tool ~860, results ~780, content 1100) plus generator inline `.seo-copy-wrap { max-width: 100% }` which let lower sections stretch full viewport.
- Fix: shared 1100px centered container + gutters in `seo-landing.css` for `body[data-page-kind="brand-page"]` (excluding serial-location pages); removed full-width override in `scripts/generate-seo-pages.js`; rebuilt generated brand pages; aligned `brand-page-template.html`.
- Branch: `fix/brand-page-desktop-layout-consistency`

## Confirmed decoder audit fixes
- Branch: `fix/confirmed-decoder-audit-issues` from `origin/main` (`f9ffa25`). Commit `a9911d9`.
- Large Loss: `buildDecodeResult` now runs `classifyDecodeResult`/`sanitizeDecodeResult`; invalid/unsupported/unknown/incomplete(non-ambiguous)/future → row errors; ambiguous multi-year ages still display. `decodeAllRows` marks error status when `result.error` is set.
- Social preview: five pages (`about`, `brands`, `disclaimer`, `find-model-serial-number`, `privacy-policy`) no longer reference missing `decode-my-item-banner.png` / `decode-my-item-logo.png`; point to existing `assets/decodemyitem-logo.png`. **No dedicated DecodeMyItem OG banner exists** — did not invent one or substitute Item Assist branding.
- Carrier: validated WWYY success now returns `month: "Week NN"` (+ `weekDigits`); year rules unchanged.
- ASUS: removed obsolete rewrite `/asus` → `asus.html`; permanent redirect `/asus` → `/asus-serial-number-decoder` remains.
- Validation: large-loss 31/31, shell-route 6/6, decoder 166/166, `npm run build` passed.
