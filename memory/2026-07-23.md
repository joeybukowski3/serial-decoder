# 2026-07-23

## Smart Lookup Groq/progressive context follow-up

- Created isolated worktree `C:\tmp\serial-decoder-smart-groq` from `origin/main` at `8f9d81bd70ee3d7cda9d0a875c3ba85c70deb753` and branch `fix/smart-lookup-progressive-context-and-groq`.
- Implemented progressive context fields, query tier handling, broad-result rendering, cache policy bump, Groq safe diagnostics, and OpenAI-to-Groq fallback metadata.
- Added small deterministic family/context seeds for high-value broad families while keeping broad brand/category claims research-eligible where possible.
- Local validation passed: `npm ci`, `npm test`, `npm run test:smart-unit`, `npm run test:smart-api`, benchmark script, `npm run build`, `git diff --check`, Smart Playwright, and targeted reruns for two general Playwright tests that timed out in parallel but passed serially.
- Browser validation warning was observed: a stale Python server occupied port 3001. It was identified and stopped before reporting Smart Playwright results against the current worktree.
- Live Groq proof is still blocked. Vercel env metadata lists `OPENAI_API_KEY` and `GROQ_API_KEY` for Preview/Production, but `vercel env pull` returned empty provider-key values while Upstash was non-empty. A safe direct Groq probe using the pulled Preview env reached Groq and returned HTTP 401 because no usable key was present.
- Do not claim the Groq provider is fixed live until a Vercel runtime/Preview call proves Groq returns structured output through the fallback path.
- Do not leave any public force-provider or cache-bypass endpoint behind. A private diagnostic token or approved Vercel env/runtime path is needed for final Preview acceptance testing.
