## 2026-05-13

- Added `api/record-decode.js` to store successful decoder outputs in Upstash Redis as permanent `decoder-verified:{brand}:{model}` records with light rate limiting and silent failure handling.
- Updated `script.js` to POST successful decode results after `renderSerialSummaryLayer()` and before `setLoadingSuccess()`, only when brand, model, and a single four-digit year are present.
- Updated `api/age-lookup.js` to check the decoder-verified Redis store after the local model-age DB and before the query cache so Smart Lookup can use verified decoder data as a higher-confidence source.
- `memory/2026-05-12.md` and prior daily files for May were missing; only today’s note was created.
