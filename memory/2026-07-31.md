# 2026-07-31

- Polished Smart Lookup results card on `feat/smart-lookup-results-card-redesign` (presentation-only): lighter typography/spacing, definition-list detail rows, restrained year panel, hero grid without vertical divider, accurate confidence labels (omit individual-unit confidence when N/A), compact timing display (`2021+`), ItemAssist referral reused from Serial Decoder (`https://itemassist.com/request-age-verification?…&source=decodemyitem`). Files: `src/browser/smart-lookup-controller.js` → rebuilt `smart-lookup-controller.js`, `smart-lookup-results.css`, redesign/unit/Playwright tests. Validation: redesign 21/21, smart unit 361/361, smart API 230/230, smart Playwright 38/38, npm test, build. Not pushed/deployed/PR.
- Created `feat/smart-lookup-results-card-redesign` from `origin/main` at `25acf7d3cbb8ef974087d7a2ca09c9550bf525e2`. Presentation-only Smart Lookup results card redesign: navy report header, blue year hero, summary panel, detail grid, What This Year Means / Things to Keep in Mind cards, accessible evidence accordions. Source of truth: `src/browser/smart-lookup-controller.js`; styles in `smart-lookup-results.css` imported via `mobile-smart-lookup.css`; rebuilt `smart-lookup-controller.js`. No backend/routing/provider changes. Validation: smart unit 354/354, smart API 230/230, npm test passed, smart Playwright 37 + redesign test, build passed. Not pushed.
- Created `feat/smart-lookup-fast-estimate-routing` from `origin/main` at `4f571361e4769247a98650e75599d3565ec93906`; local `main` was not modified.
- Refactored Smart Lookup age routing to prefer local/verified evidence, Redis, generalized Serper+Gemini shared evidence, one selected heavyweight provider, then a deterministic estimate.
- Shared evidence now supports exact-model, model-line, product-family, and meaningful partial-model tiers with tier-specific evidence acceptance and precision.
- The normal API route selects OpenAI by default or xAI via `SMART_LOOKUP_HEAVY_PROVIDER=xai`; it explicitly disables sequential OpenAI-to-xAI fallback.
- Added a canonical estimate-first provider prompt, additive estimate fields, 15-second global deadline, 3-second aggregate Serper budget, 4-second Gemini extraction target, and 6.5-second heavyweight cap.
- Bumped Smart Lookup age cache policy to v6, uses 180-day cited-result caching, and added 15-minute negative caching for failed shared retrieval and useful deterministic provider-failure substitutes.
- Added routing telemetry and corrected per-request provider-attempt reporting so cumulative daily counters are named separately.
- Added focused tests and a 12-query mocked routing/cache benchmark. Final validation: focused 16/16, benchmark 4/4, Smart unit 332/332, Smart API 213/213, full `npm test` passed, and `npm run build` passed. No lint or typecheck scripts exist.
- Shadow recommendation: leave safe during deployment validation, then set `SMART_LOOKUP_SHARED_MODEL_EVIDENCE_SHADOW_ENABLED=false`; active and shadow paths are guarded against duplicate shared calls.
- Committed the implementation as `ca512a3` (`Refactor Smart Lookup estimate routing`). No PR was opened and nothing was pushed.
- Stabilized the serial-refinement browser timing test by replacing the redundant `Date.now()` wall-clock check with a 750 ms Playwright visibility timeout; the targeted test passed 1/1, the full suite passed 12/12, and the test-only fix was committed as `a240103` (`test: stabilize serial refinement browser timing`). No PR action was taken.
- Added exact local VIZIO M321i-A2 model-generation evidence (2013 generation, estimated 2013-2014 production window), strict M321i/M32li label-ambiguity handling, estimate-preserving LG/VIZIO conflict behavior, model-production schema semantics, and decoded-results UI disclosure/confidence details. Unsupported VIZIO suffixes do not inherit the mapping, and serial evidence still outranks model-generation estimates.
- Validation for the VIZIO work: focused API/UI 32/32, broader focused 139/139, decoder 154/154, Smart unit 333/333, Smart API 219/219, Smart Playwright 37/37, full `npm test` passed, and build passed. The separate broad Playwright command could not connect because it assumes an externally started server; `npx vercel dev` is recursively configured through the package dev script, and Windows sandbox process spawning prevented the temporary local-server workaround. Manual handler validation confirmed zero Redis/Serper/Gemini/OpenAI/xAI calls for LG / LWJ20PAP1801284 / M321i-A2.
- Expanded deterministic VIZIO TV coverage into `data/vizio-tv-generations.json` with a dedicated normalization/resolver layer and documented official evidence. The registry contains 13 exact records and 13 constrained lineup patterns covering 93 canonical models across 2010, 2013-2016, 2018-2019, and 2021-2023; no suffix-only year rule exists. Integrity validation checks duplicate models/aliases, invalid or overlapping regexes, evidence references, ranges, and estimate bases. Benchmark: 123/123 canonical/alias inputs resolved locally, 93 provider calls avoided, 0/8 unsupported false positives, ~0.20 ms average resolver time. Focused VIZIO tests 13/13, decoder 154/154, Smart API 221/221, full `npm test` passed, build passed, targeted VIZIO Playwright 1/1 and Smart Playwright 37/37 passed. Broad Playwright remained infrastructure-blocked because it has no owned server and all 45 attempts received `ERR_CONNECTION_REFUSED` at localhost:3001.
- Fixed estimate-first timeout behavior for recognizable product families/model lines. Root cause for "Lenovo ThinkSystem ST50": no family registry recognition (brand-only partial ST50), weak incomplete-token reserve with no year, and UI `classifyAgeOutcome` prioritized `PROVIDER_TIMEOUT`/`TOTAL_DEADLINE` over usable age info, discarding the payload and showing "stopped before guessing." Added Lenovo ThinkSystem ST50 model-line seed (original 2018-2023; V2/V3 recognized without inventing years; 7Y48/7Y49 map to original). UI now prefers usable estimates over pure timeout copy; deterministic degraded wording uses estimate-first language. Focused ST50 tests 9/9, Smart unit 340/340, Smart API 230/230, Smart Playwright 37/37, full `npm test` and build passed. Committed as `fix: preserve estimates when Smart Lookup research times out`. No push/PR.

- Implemented Phase 1 on `feat/unified-model-identity-refinement-ladder` from origin/main `25acf7d`: shared model identity (O/0, I/1), dual-form Serper search, canonical-equivalent scoring, serial refinement result ladder (ranked / ambiguous_with_era), timeout degradation, UI disclosure. Commit `377a7a4`. Not pushed/deployed/PR. Recommend `MODEL_REFINEMENT_MODE=deterministic_serper` for production after validation; legacy_gemini retained.
- Phase 2 on `feat/refinement-production-hardening` from origin/main `ff7b7de` (#76): failure taxonomy, mode-aware budgets (browser 15s / det API 12s / legacy API 14s), standardized telemetry + cost proxies, cache v3 + mode/canonical keys, singleflight, canary (`npm run test:lookup-canary`), cross-workflow parity fixtures, Playwright ranked/era/timeout/conflict/mobile, production runbook `docs/serial-refinement-production-hardening.md`. Do not flip env in same deploy as code. Conditional GO for `deterministic_serper` after two-step deploy + smoke. Not pushed/deployed/PR.
