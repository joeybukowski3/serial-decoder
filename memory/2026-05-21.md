# 2026-05-21

- Smart lookup result cards no longer show the year row when a `Production Range` is present.
- Implemented a shared `buildSmartLookupResultFields()` helper in `script.js` and reused it in both `showAgeLookupResults()` and `buildProgressiveAgeLookupMarkup()`.
- Intent: avoid redundant or misleading `Median Year` / midpoint-style year output when the production window already communicates the useful timing information.
