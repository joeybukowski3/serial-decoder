# 2026-07-11

## Smart Lookup LG OLED product-family recall

- Confirmed `feature/smart-lookup-model-recall-fixes` was merged as PR #36 at `c6e46f6`; latest fetched `origin/main` was `65ee705` (PR #37 Richmond merge).
- Kept Richmond work out of the Smart Lookup diff by using isolated worktree `C:\tmp\serial-decoder-lg-c3` and branch `feature/smart-lookup-lg-product-family-recall` from `origin/main`.
- Root cause for `LG C3 TV`: the generic model heuristic requires four characters, the deterministic TV-family seed covered Samsung Q-series only, and the legacy local alias `C3` could incorrectly substitute `OLED55C3PUA`.
- Added brand/category-scoped LG OLED seeds for B3, C2, C3, and G3; safe exact LG OLED model parsing/inference; deterministic partial family/exact-model context; product-family and exact-model UI buckets; generated browser controller; recall/API/UI/Playwright tests.
- `LG C3 TV` now returns `partial` / `product-family-recognized`, exact model and unit manufacture year null, with 2023 labeled only as model-year-family context. `LG OLED65C3PUA` preserves the exact model and screen size 65.
- Commit: `ba2bb66` (`fix: recognize LG OLED product families in Smart Lookup`). Fast-forward pushed directly to `origin/main` (`65ee705..ba2bb66`) after Joey explicitly requested it.
- Passed Smart unit (61), Smart API (20), Smart Playwright (15), Smart benchmark (1), full `npm test` (129 decoder + 35 refinement + 33 API), `npm run build`, requested syntax checks, and `git diff --check`.
